# ReactRush

Zoom to 200% (CTRL +) for the best performance

ReactRush is an interactive reaction-time mini-game. Users must tap the panel as soon as it turns green. The game measures reaction speed, prevents early clicks, stores best scores, and displays recent attempts.
